<page>
    <list>
        <list-item arrow a:for="{{components}}" key="{{index}}">
            <navigator url="{{item.url}}">{{ item.name }}</navigator>
        </list-item>
    </list>
    <view slot="footer">
        <button type="primary">Button</button>
    </view>
</page>