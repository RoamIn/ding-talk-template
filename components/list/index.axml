<view class="e-list {{className}}">
  <view class="e-list-header" a:if="{{$slots.header}}">
    <slot name="header" />
  </view>
  <view class="e-list-body">
    <slot />
  </view>
  <view class="e-list-footer" a:if="{{$slots.footer}}">
    <slot name="footer" />
  </view>
</view>
